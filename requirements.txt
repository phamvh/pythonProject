# To generate this file from an existing project, go into the dir of the project and activate the virtual env.
# A simply way to do that is, use the terminal of pycharm and you are automatically in the virtual env.
# Then run:
#      `pip3 freeze > requirements.txt`
#
# Similarly, if you want to install all modules listed in the requirements.txt, run:
#      `pip3 install -r requirements.txt`
attrs==22.2.0
contourpy==1.0.7
cycler==0.11.0
exceptiongroup==1.1.0
fonttools==4.38.0
iniconfig==2.0.0
kiwisolver==1.4.4
matplotlib==3.6.3
numpy==1.24.1
ordered-set==4.1.0
packaging==23.0
pandas==1.5.3
Pillow==9.4.0
pluggy==1.0.0
pyparsing==3.0.9
pytest==7.2.1
python-dateutil==2.8.2
pytz==2022.7.1
six==1.16.0
tomli==2.0.1
